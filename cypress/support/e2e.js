import './commands'
import 'cypress-mochawesome-reporter/register'

// Optionally put global configuration and behavior that modifies Cypress here.
// For example, you can set retry rules, global hooks, etc.

// Example: preserve cookies between tests if needed
// Cypress.Cookies.defaults({ preserve: 'session_id' })
